{
  "name": "test-wpp",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "complete": "rmdir /s /q node_modules && npm cache clean --force && npm ci && npm start",
    "completeLinux": "rm -rf node_modules && npm cache clean --force && npm ci && npm start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.3",
    "qrcode-terminal": "^0.12.0",
    "whatsapp-web.js": "^1.16.4"
  }
}
